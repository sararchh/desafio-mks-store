export interface CartProps {
    id: number,
    name: string,
    qtd: number;
    subtotal?: number;
    description: string,
    brand: string,
    price: string,
    photo: string,
    createdAt: string,
    updatedAt: string,
}